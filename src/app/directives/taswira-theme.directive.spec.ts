import { TaswiraThemeDirective } from './taswira-theme.directive';

describe('TaswiraThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new TaswiraThemeDirective();
    expect(directive).toBeTruthy();
  });
});
