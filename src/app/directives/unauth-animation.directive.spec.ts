import { UnauthAnimationDirective } from './unauth-animation.directive';

describe('UnauthAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new UnauthAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
