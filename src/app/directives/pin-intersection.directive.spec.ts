import { PinIntersectionDirective } from './pin-intersection.directive';

describe('PinIntersectionDirective', () => {
  it('should create an instance', () => {
    const directive = new PinIntersectionDirective();
    expect(directive).toBeTruthy();
  });
});
