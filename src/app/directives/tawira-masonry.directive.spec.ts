import { TawiraMasonryDirective } from './tawira-masonry.directive';

describe('TawiraMasonryDirective', () => {
  it('should create an instance', () => {
    const directive = new TawiraMasonryDirective();
    expect(directive).toBeTruthy();
  });
});
